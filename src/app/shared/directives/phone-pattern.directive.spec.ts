import { PhonePatternDirective } from './phone-pattern.directive';

describe('PhonePatternDirective', () => {
  it('should create an instance', () => {
    const directive = new PhonePatternDirective();
    expect(directive).toBeTruthy();
  });
});
